---
/**
 * Products page CUPCAKES grid. Figma 3311:2985 rows at y=1227..4082.
 *
 * Renders a 3-column responsive grid (1 / 2 / 3) of CupcakeFlavorCard.
 * The H2 is the in-page anchor target (#cupcakes) used by the
 * CategoryHeader link.
 */
import CupcakeFlavorCard from "./CupcakeFlavorCard.astro";

type FlavorKey =
  | "chocolate"
  | "vanilla"
  | "carrot"
  | "butter-pecan"
  | "pumpkin"
  | "lemon"
  | "strawberry";

interface Flavor {
  key: FlavorKey;
  name: string;
  description: string;
  price: string;
  imageSrc: string;
  imageAlt: string;
  orderHref: string;
}

interface Props {
  heading: string;
  flavors: ReadonlyArray<Flavor>;
}

const { heading, flavors } = Astro.props;
---

<section
  id="cupcakes"
  aria-labelledby="products-cupcakes-heading"
  class="w-full bg-white
         scroll-mt-24
         px-4 sm:px-6 md:px-10 lg:px-14
         py-10 sm:py-14 md:py-20 lg:py-24"
  data-node-id="3311:2985"
  data-name="CUPCAKES grid"
>
  <div class="mx-auto max-w-screen-2xl flex flex-col items-center
              gap-8 sm:gap-10 md:gap-12 lg:gap-16">
    <h2
      id="products-cupcakes-heading"
      class="m-0 text-center text-black font-normal leading-[1.125]
             text-[3.85rem] sm:text-[4.62rem] md:text-[5.39rem] lg:text-[6.16rem] xl:text-[92.4px]"
    >{heading}</h2>

    <div class="grid grid-cols-1 sm:grid-cols-3
                gap-[1.35rem] sm:gap-[1.8rem] md:gap-[2.25rem] w-full">
      {flavors.map((f) => (
        <CupcakeFlavorCard
          flavorKey={f.key}
          name={f.name}
          description={f.description}
          price={f.price}
          imageSrc={f.imageSrc}
          imageAlt={f.imageAlt}
          orderHref={f.orderHref}
        />
      ))}
    </div>
  </div>
</section>
